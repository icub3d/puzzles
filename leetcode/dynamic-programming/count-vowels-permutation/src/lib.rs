pub struct Solution;

enum State {
    A,
    E,
    I,
    O,
    U,
}

impl Solution {
    pub fn count_vowel_permutation(n: i32) -> i32 {}
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
